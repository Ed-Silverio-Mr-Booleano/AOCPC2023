#include <bits/stdc++.h>
#define ll long long
using namespace std;

void searchPair(int k, int v[], int l, int r){
    while(l <= r){
        int mid = (l+2)/2;
        if(v[mid] == v[])
    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin>> n;

    cout << contarTuplas(n)<<endl;


}
