#include <bits/stdc++.h>
typedef long long ll;
#define fast ios_base::sync_with_stdio(false), cin.tie(nullptr)

using namespace std;

int main(){
    fast;
    ll n, q;
    cin>>q;
    while(q--){
        cin>>n;
        ll s = 0;
        for(int i = 1; i <= n; ++i){
            s += i; 
        }
    }
    return 0;
}